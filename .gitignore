*.zip
*.pyc
__pycache__/
.cache/
.upm/
venv/
.venv/
*.log

# SEGURANCA: contem hashes de senha e tokens de sessao ativos.
# Em prod sao armazenados no Postgres via kvstore — disco eh so seed/cache.
data/users.json
data/sessions.json

# Backup local antes de alteracoes (evita commitar pelado)
server_backup_*.py
index_backup*.html
